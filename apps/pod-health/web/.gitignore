/node_modules
/dist
*.tsbuildinfo

# Generated at build from APP_ORIGIN by scripts/gen-clientid.mjs — never committed
# (per-origin artifacts; the source of truth is the script + APP_ORIGIN env).
/public/clientid.jsonld
/public/callback.html

# Local env overrides
.env
.env.local

# Playwright e2e artifacts + the global-setup seeded-account sidecar (regenerated
# every run by tests/e2e/global-setup.ts — never committed).
/test-results
/playwright-report
/tests/e2e/.seeded-account.json
