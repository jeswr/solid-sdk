## API Report File for "@jeswr/solid-agent-card"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { DatasetCore } from '@rdfjs/types';
import { Quad } from '@rdfjs/types';

// @public
export const A2A_PROTOCOL_VERSION: "0.3.0";

// @public
export const AGENT_POINTER_PREDICATES: readonly ["http://www.w3.org/ns/solid/interop#hasAuthorizationAgent", "https://schema.org/agent", "http://schema.org/agent"];

// @public
export interface AgentCard {
    readonly "x-solid"?: {
        readonly owner?: string;
        readonly agentDescription?: string;
        readonly protocolSources?: readonly string[];
    };
    // (undocumented)
    readonly description?: string;
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly preferredTransport?: string;
    // (undocumented)
    readonly protocolVersion: string;
    // (undocumented)
    readonly securitySchemes?: Readonly<Record<string, AgentCardSecurityScheme>>;
    // (undocumented)
    readonly skills?: readonly {
        readonly id: string;
        readonly name: string;
        readonly description?: string;
        readonly tags?: readonly string[];
    }[];
    // (undocumented)
    readonly url: string;
}

// @public
export interface AgentCardSecurityScheme {
    // (undocumented)
    readonly description?: string;
    // (undocumented)
    readonly openIdConnectUrl?: string;
    // (undocumented)
    readonly type: string;
}

// @public
export function agentCardUrl(origin: string): string;

// @public
export interface AgentDescriptionDocument {
    readonly quads: readonly Quad[];
    toJsonLd(): Promise<Record<string, unknown>>;
    toTurtle(format?: string): Promise<string>;
}

// @public
export function agentDescriptionsUrl(origin: string): string;

// @public
export interface AgentDescriptor {
    readonly description?: string;
    readonly did?: string;
    readonly id: string;
    readonly name: string;
    readonly owner?: string;
    readonly protocolSources?: readonly string[];
    readonly securitySchemes?: readonly SecurityScheme[];
    readonly skills?: readonly AgentSkill[];
    readonly url?: string;
}

// @public
export interface AgentDescriptorDocuments {
    readonly agentCard: AgentCard;
    readonly agentDescription: AgentDescriptionDocument;
}

// @public
export interface AgentDiscovery {
    readonly descriptor?: AgentDescriptor;
    readonly pointers: readonly AgentPointer[];
    readonly verification?: VerificationResult;
    readonly webId: string;
}

// @public
export interface AgentPointer {
    readonly agent: string;
    readonly predicate: string;
    readonly webId: string;
}

// @public
export interface AgentPointerDocument {
    readonly quads: readonly Quad[];
    toString(format?: string): Promise<string>;
}

// @public
export interface AgentPointerOptions {
    readonly predicate?: PointerPredicate;
}

// @public
export interface AgentSkill {
    readonly description?: string;
    readonly id: string;
    readonly name: string;
    readonly tags?: readonly string[];
}

// @public
export const ANP_AD: "https://w3id.org/agent-description#";

// @public
export const ANP_CONTEXT_URL: "https://w3id.org/agent-description/v1";

// @public
export const ANP_INLINE_CONTEXT: Readonly<Record<string, unknown>>;

// @public
export function buildAgentPointer(webId: string, agent: string, predicates?: PointerPredicate | readonly PointerPredicate[]): AgentPointerDocument;

// @public
export function describeAgent(descriptor: AgentDescriptor): AgentDescriptorDocuments;

// @public
export function discoverAgent(webId: string, options?: DiscoverOptions): Promise<AgentDiscovery>;

// @public
export interface DiscoverOptions {
    readonly fetch?: typeof globalThis.fetch;
    readonly resolveDescriptor?: boolean;
}

// @public
export const HAS_AUTHORIZATION_AGENT: "http://www.w3.org/ns/solid/interop#hasAuthorizationAgent";

// @public
export type PointerPredicate = "interop:hasAuthorizationAgent" | "schema:agent";

// @public (undocumented)
export const SCHEMA_AGENT: "https://schema.org/agent";

// @public
export const SECURITY_SCHEME_TYPES: readonly ["solid-oidc", "public", "bearer", "oauth2"];

// @public
export interface SecurityScheme {
    readonly description?: string;
    readonly issuer?: string;
    readonly type: SecuritySchemeType;
}

// @public
export type SecuritySchemeType = (typeof SECURITY_SCHEME_TYPES)[number];

// @public
export function serialize(quads: readonly Quad[], format?: string): Promise<string>;

// @public
export const VALID_SECURITY_SCHEME_TYPES: ReadonlySet<string>;

// @public
export interface VerificationIssue {
    readonly code: VerificationIssueCode;
    readonly message: string;
    readonly subject?: string;
    readonly value?: string;
}

// @public
export type VerificationIssueCode = "no-agent-description" | "multiple-agent-descriptions" | "subject-mismatch" | "missing-name" | "missing-url" | "invalid-url" | "invalid-owner" | "skill-missing-id" | "skill-missing-name" | "duplicate-skill-id" | "invalid-security-scheme" | "invalid-protocol-source" | "fetch-failed" | "parse-failed";

// @public
export interface VerificationResult {
    readonly descriptor?: AgentDescriptor;
    readonly issues: readonly VerificationIssue[];
    readonly valid: boolean;
}

// @public
export function verifyDataset(dataset: DatasetCore, expectedId?: string, options?: VerifyDatasetOptions): VerificationResult;

// @public
export interface VerifyDatasetOptions {
    readonly requireSubjectMatch?: boolean;
}

// @public
export function verifyDescriptor(input: string, options?: VerifyOptions): Promise<VerificationResult>;

// @public
export interface VerifyOptions {
    readonly baseIRI?: string;
    readonly body?: string;
    readonly bodyContentType?: string;
    readonly expectedId?: string;
    readonly fetch?: typeof globalThis.fetch;
    readonly requireSubjectMatch?: boolean;
}

// @public
export const WELL_KNOWN_AGENT_CARD: "/.well-known/agent-card.json";

// @public
export const WELL_KNOWN_AGENT_DESCRIPTIONS: "/.well-known/agent-descriptions";

```
