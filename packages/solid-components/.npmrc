# Supply-chain hardening (suite default, mirrors prod-solid-server ADR-0011):
# no npm lifecycle hook runs on install. Defends against malware shipping its
# payload in a postinstall/preinstall/prepare hook. The committed dist/ means a
# consumer never needs to run this package's build under ignore-scripts=true.
ignore-scripts=true
