{
  "name": "@jeswr/matrix-chat-to-pod",
  "version": "0.0.1",
  "description": "Ingest Matrix room history into a Solid pod (owner-private, READ/import-only). The highest chat-capture multiplier: one schema reaches WhatsApp/Signal/Telegram/Slack/Discord/iMessage via mautrix bridges/Beeper. A pure, fixture-tested transform maps a Matrix m.room.message event to a @jeswr/solid-chat-interop CanonicalMessage (edits via m.replace -> dct:isReplacedBy, redactions -> schema:dateDeleted, replies via m.in_reply_to), then writes each as a SolidOS LongChat resource readable by PM /chat and any LongChat reader. Homeserver fetches are SSRF-guarded via @jeswr/guarded-fetch; the pod-write fetch is injectable.",
  "type": "module",
  "license": "MIT",
  "author": "Jesse Wright",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jeswr/matrix-chat-to-pod.git"
  },
  "homepage": "https://github.com/jeswr/matrix-chat-to-pod#readme",
  "bugs": {
    "url": "https://github.com/jeswr/matrix-chat-to-pod/issues"
  },
  "keywords": [
    "solid",
    "rdf",
    "matrix",
    "chat",
    "mautrix",
    "beeper",
    "longchat",
    "activitystreams",
    "import",
    "interop",
    "ssrf"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./transform": {
      "types": "./dist/transform.d.ts",
      "import": "./dist/transform.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "biome check src test",
    "lint:fix": "biome check --write src test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@jeswr/fetch-rdf": "^0.1.0",
    "@jeswr/guarded-fetch": "workspace:*",
    "@jeswr/rdf-serialize": "workspace:*",
    "@jeswr/solid-chat-interop": "workspace:*",
    "n3": "^2.0.3"
  },
  "peerDependencies": {
    "@rdfjs/types": "^2.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@rdfjs/types": "^2.0.1",
    "@types/n3": "^1.26.1",
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=24"
  }
}
