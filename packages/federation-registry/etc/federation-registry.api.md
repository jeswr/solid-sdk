## API Report File for "@jeswr/federation-registry"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { DatasetCore } from '@rdfjs/types';
import type { Quad } from '@rdfjs/types';

// @public
export function acceptsSpec(storage: Pick<StorageDescription, "acceptsSpec">, specVersionIri: string): boolean;

// @public
export function buildMembership(input: MembershipInput & {
    id: string;
}): BuiltGraph;

// @public
export function buildRegistry(input: RegistryInput): BuiltGraph;

// @public
export interface BuiltGraph {
    readonly quads: readonly Quad[];
    toString(format?: string): Promise<string>;
}

// @public
export interface BuiltStorage {
    readonly quads: readonly Quad[];
    toString(format?: string): Promise<string>;
}

// @public
export const DCAT: "http://www.w3.org/ns/dcat#";

// @public
export function describeStorage(input: StorageInput): BuiltStorage;

// @public
export const FEDAPP: "https://w3id.org/jeswr/fed#";

// @public
export const FEDREG: "https://w3id.org/jeswr/fedreg#";

// @public
export interface FetchOptions {
    readonly baseIRI?: string;
    readonly body?: string;
    readonly bodyContentType?: string;
    readonly fetch?: typeof globalThis.fetch;
}

// @public
export function listMembers(input: string, options?: FetchOptions): Promise<MembershipVerification[]>;

// @public
export interface Membership {
    readonly app: string;
    readonly asserted?: string;
    readonly assertedBy?: readonly string[];
    readonly id?: string;
    readonly status?: MembershipStatusName;
    readonly statusIri?: string;
}

// @public
export const MEMBERSHIP_STATUS: {
    readonly Proposed: "https://w3id.org/jeswr/fedreg#Proposed";
    readonly Active: "https://w3id.org/jeswr/fedreg#Active";
    readonly Suspended: "https://w3id.org/jeswr/fedreg#Suspended";
    readonly Revoked: "https://w3id.org/jeswr/fedreg#Revoked";
};

// @public
export interface MembershipInput {
    readonly app: string;
    readonly asserted?: string;
    readonly assertedBy: string | readonly string[];
    readonly id?: string;
    readonly status?: MembershipStatusName;
}

// @public
export type MembershipStatusName = keyof typeof MEMBERSHIP_STATUS;

// @public
export interface MembershipVerification {
    readonly issues: readonly RegistryIssue[];
    readonly membership?: Membership;
    readonly valid: boolean;
}

// @public
export interface ParsedRegistry {
    readonly issues: readonly RegistryIssue[];
    readonly members: readonly MembershipVerification[];
    readonly registry?: Registry;
    readonly valid: boolean;
}

// @public
export function parseRegistry(input: string, options?: FetchOptions): Promise<ParsedRegistry>;

// @public
export function parseRegistryDataset(dataset: DatasetCore, expectedId?: string): ParsedRegistry;

// @public
export function parseStorage(input: string, options?: StorageFetchOptions): Promise<StorageVerification>;

// @public
export function parseStorageDataset(dataset: DatasetCore, expectedId?: string): StorageVerification;

// @public
export interface Registry {
    readonly id: string;
    readonly members: readonly Membership[];
}

// @public
export interface RegistryInput {
    readonly id: string;
    readonly members: readonly MembershipInput[];
}

// @public
export interface RegistryIssue {
    readonly code: RegistryIssueCode;
    readonly message: string;
    readonly subject?: string;
    readonly value?: string;
}

// @public
export type RegistryIssueCode = "no-registry" | "multiple-registries" | "no-membership" | "membership-missing-app" | "membership-multiple-apps" | "membership-missing-status" | "membership-multiple-statuses" | "unknown-status" | "membership-missing-asserted-by" | "invalid-term-type" | "no-storage-description" | "storage-missing-accepts-spec" | "fetch-failed" | "parse-failed";

// @public
export const SECTOR_BASE: "https://w3id.org/jeswr/sectors/";

// @public
export function serialize(quads: readonly Quad[], format?: string): Promise<string>;

// @public
export function statusName(iri: string): MembershipStatusName | undefined;

// @public
export interface StorageDescription {
    readonly acceptsSpec: readonly string[];
    readonly id: string;
    readonly storage: string;
    readonly supportsSector: readonly string[];
}

// @public
export interface StorageFetchOptions {
    readonly baseIRI?: string;
    readonly body?: string;
    readonly bodyContentType?: string;
    readonly fetch?: typeof globalThis.fetch;
}

// @public
export interface StorageInput {
    readonly acceptsSpec: readonly string[];
    readonly id: string;
    readonly storage?: string;
    readonly supportsSector?: readonly string[];
}

// @public
export interface StorageVerification {
    readonly issues: readonly RegistryIssue[];
    readonly storage?: StorageDescription;
    readonly valid: boolean;
}

// @public
export const TRUSTED_STATUS: ReadonlySet<MembershipStatusName>;

// @public
export function unsupportedSpecs(storage: Pick<StorageDescription, "acceptsSpec">, wanted: readonly string[]): string[];

// @public
export const VALID_STATUS_IRIS: ReadonlySet<string>;

// @public
export function verifyMembership(input: string, options?: FetchOptions): Promise<MembershipVerification>;

// @public
export function verifyMembershipDataset(dataset: DatasetCore): MembershipVerification;

```
