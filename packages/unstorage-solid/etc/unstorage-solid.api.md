## API Report File for "@jeswr/unstorage-solid"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { Driver } from 'unstorage';

// @public
const solidDriver: (opts: SolidDriverOptions) => Driver<SolidDriverOptions, undefined>;
export default solidDriver;

// @public
interface SolidDriverOptions {
    base: string;
    defaultContentType?: string;
    fetch?: typeof globalThis.fetch;
    headers?: Record<string, string>;
    // @internal
    onWatchDegrade?: (reason: string) => void;
    watch?: boolean;
    // @internal
    wsFactory?: WatchSocketFactory;
}
export { SolidDriverOptions as Options }
export { SolidDriverOptions }

// @public
export class SolidHttpError extends Error {
    constructor(method: string, url: string, status: number, statusText: string);
    // (undocumented)
    readonly status: number;
    // (undocumented)
    readonly url: string;
}

// @public
export class SolidPreconditionFailedError extends Error {
    constructor(url: string, status: number);
    // (undocumented)
    readonly status: number;
    // (undocumented)
    readonly url: string;
}

// @public
export class SolidRedirectError extends Error {
    constructor(url: string, status: number);
    // (undocumented)
    readonly status: number;
    // (undocumented)
    readonly url: string;
}

// @public
export interface WatchSocket {
    // (undocumented)
    addEventListener(type: "message", listener: (ev: {
        data: unknown;
    }) => void): void;
    // (undocumented)
    addEventListener(type: "error", listener: (ev: unknown) => void): void;
    // (undocumented)
    addEventListener(type: "close", listener: (ev: unknown) => void): void;
    // (undocumented)
    close(): void;
}

// @public
export type WatchSocketFactory = (url: string) => WatchSocket;

```
